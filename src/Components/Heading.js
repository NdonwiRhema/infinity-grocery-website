import React from 'react'

const Heading = ({text}) => {
  return (
   <div style={{maxWidth:'50%',margin:'2rem',borderBottom:'2px solid #f39317'}}>
    <h4 >{text}</h4>
  </div>
  )
}

export default Heading